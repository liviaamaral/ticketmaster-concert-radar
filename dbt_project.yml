name: "ticketmaster_concert_radar"
version: "1.0.0"
config-version: 2

profile: "ticketmaster_concert_radar"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ticketmaster_concert_radar:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
